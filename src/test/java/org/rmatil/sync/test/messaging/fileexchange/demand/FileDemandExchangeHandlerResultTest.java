package org.rmatil.sync.test.messaging.fileexchange.demand;

import org.junit.Test;
import org.rmatil.sync.core.messaging.fileexchange.demand.FileDemandExchangeHandlerResult;

public class FileDemandExchangeHandlerResultTest {

    @Test
    public void test() {
        FileDemandExchangeHandlerResult result = new FileDemandExchangeHandlerResult();
    }
}
