package org.rmatil.sync.test.messaging.fileexchange.move;

import org.junit.Test;
import org.rmatil.sync.core.messaging.fileexchange.move.FileMoveExchangeHandlerResult;

public class FileMoveExchangeHandlerResultTest {

    @Test
    public void test() {
        FileMoveExchangeHandlerResult fileMoveExchangeHandlerResult = new FileMoveExchangeHandlerResult();
    }
}
