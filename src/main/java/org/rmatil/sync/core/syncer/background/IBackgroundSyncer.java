package org.rmatil.sync.core.syncer.background;

/**
 * Defines the interface for a background syncer which is ran
 * periodically on all clients to reconcile the synchronized folder
 * amongst them.
 */
public interface IBackgroundSyncer extends Runnable {

}
